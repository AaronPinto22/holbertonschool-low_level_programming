/**
 * puts prints out a line with a new line at the end
 */

#include <stdio.h>

int main(void)
{
/**
 * main returns 0
 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
